/* import { getClient, readDir, createDir } from './utils';

export let api = {
  createDir: (e, path: string) => {
    return createDir(e, path);
  },
  readDir: (e, path: string) => {
    return readDir(e, path);
  }
}; */



