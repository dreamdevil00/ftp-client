/* import { getClient } from './utils';
import * as Promise from 'bluebird';

export let api = {
  login: (e, credentials): Promise<any> => {
    const client = getClient();
    return client.connect();
  },
  logout: (cb) => {
    cb(null, {msg: 'ok'});
  }
}; */

